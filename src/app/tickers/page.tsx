export default function Home({searchParams, params}: {searchParams: any, params: any}) {
  console.log("searchParamss::", searchParams, params)
  return (
    <h1  style={{ color: 'red' }}>
      Vamos ver se da cerfto Tickers
    </h1>
  );
}
